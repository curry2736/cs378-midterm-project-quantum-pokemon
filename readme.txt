To run the game, run "python ArisreiGraphics.py"

The following packages need to be installed:
pip install numpy
pip install matplotlib 
pip install PySide6 
pip install qiskit

The code runs significantly easier on Mac or Linux than on Windows.